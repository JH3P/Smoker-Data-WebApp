READ ME:

In order to run this application, please find the SmokerDataApplication.jar file in the target folder.

Simply use 'java -jar SmokerDataApplication.jar' to run, and then CTRL (or Command) + C to end the application.

This application is a simple example of taking a JSON file as the backend and serving up data with a 
REST web service using the Jersey framework.

The front end was built using the following libraries:
-AngularJS
-Bootstrap
-Angular-NVD3 (D3, NVD3)
-Angular-Resource 

This application graphically visualizes smoker trend data.
